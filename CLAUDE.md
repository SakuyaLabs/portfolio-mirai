# Project: 株式会社ミライ工務店

> このファイルは `sakuyalabs_portfolio_plan.md`(SakuyaLabs Web Portfolio Project 企画書)のCLAUDE.md雛形をミライ工務店用に埋めたものです。
> 11案件中⑤で、証明する能力軸は「コーポレートの型を崩さない安定感」。①〜④が業種ごとに強い世界観を演出してきたのに対し、
> 本案件はあえて奇をてらわない設計にする。これ自体が「制作者の作家性を出しすぎない判断ができる」ことの証明。
> 詳細な要件定義・IA・意思決定ログは `docs/planning.md` を参照してください。

## クライアント概要
- 業種：建築・リフォーム会社（従業員15名、地域密着）
- 屋号：株式会社ミライ工務店
- ターゲットペルソナ（2軸）：
  - 発注検討者（施主）：40〜60代。堅実そうな会社かどうかを重視する。
  - 採用検討者（協力会社・求職者）：30〜40代の職人・現場スタッフ。会社の安定感を知りたい。
- ゴール（CV地点）：施主軸＝施工相談・見積り依頼（お問い合わせ）、採用軸＝採用ページへの導線

## サイト構成
- `/`：TOPページ（Hero→事業内容→施工事例抜粋→会社概要抜粋→採用情報導線→お知らせ抜粋→最終CTA）
- `/works`：施工事例一覧（新築／リフォーム／リノベのカテゴリフィルタ付き）
- `/about`：会社概要（沿革・代表挨拶・企業理念）
- `/careers`：採用情報（簡易。⑥Nolanへの導線として最小限）
- `/news`、`/news/[slug]`：お知らせ一覧・詳細（`[slug]`のみSSG）
- `/contact`：お問い合わせ

## デザインルール
- カラー（`src/app/globals.css` の `@theme` に定義）：
  - Primary（鉄色）: `--color-iron: #3A332C`
  - Secondary（オフホワイト）: `--color-white-off: #F5F3EF`
  - Accent（深緑）: `--color-green: #4B6B4F`（明背景ではそのまま使用可、コントラスト比5.39）
    - 暗背景でのテキスト用: `--color-green-soft: #87AB8C`（iron上でコントラスト比4.87）
  - Neutralは`iron`から派生したグレースケール
  - 配色は実装着手前にコントラスト比を計算し、明背景用・暗背景用のトークンを最初から分けて設計する
    （③FORGE以降の標準方針）
- フォント（`next/font/google`）：**Noto Sans JP一本**（見出し・本文とも同一書体、ウェイトのみ変える）。
  他案件のような装飾的な見出しフォントはあえて使わない
- 余白の基準：8pxグリッド／セクション間 96px
- ボタン：角丸は最小限（4px程度）。ホバーは彩度変化・拡大を行わず、明度変化のみに留める

## 技術スタック
- Next.js 16 (App Router) / TypeScript / Tailwind CSS v4（CSS-first設定）
- 施工事例・お知らせは、型定義とデータ取得関数を分離し、microCMS等の外部CMSへ差し替えやすい
  スキーマ設計にする（企画書⑤の技術ポイント。コード内コメントで設計意図を明示すること）
- 施工事例一覧のカテゴリフィルタはクライアント側`useState`で実装
- `/news/[slug]`のみ`generateStaticParams`によるSSG。他ページは通常の静的ページ
- アニメーション：Framer Motion（①〜④の中で最も控えめ。単純なフェードインのみ）
- フォーム：React Hook Form + Zod（お問い合わせフォーム）
- ビジュアル素材：写真は使用せず、CSS/SVGによる抽象ビジュアル（建材のグリッド・直線的な意匠）で
  世界観を表現する（①〜④と共通方針）

## 実装時の絶対ルール
- コンポーネントは Atomic に分割し、1ファイル1責務
- 写真を使う場合は必ず `next/image`（今回は未使用の想定）
- アクセシビリティ：全インタラクティブ要素にフォーカスリング、`prefers-reduced-motion` 対応必須
- `<dl>` を使う場合は dt/dd を div でラップしない（Fragmentでグループ化。①NAGI Phase 5の反省点）
- SVGフィルター（feTurbulence等）は使う場合、小タイル化して `<pattern>` で敷き詰める（①NAGI Phase 5の反省点）
- 複数列グリッドは `grid-cols-1` ベースで `sm:`/`lg:` を使い拡張する（④はる法律事務所Phase 5の反省点、
  実装時点で守ること。事後の一括修正を避ける）
- 各ページに `<h1>` を最初から正しく設定する
- レスポンシブは 375 / 768 / 1440 の3ブレークポイントで確認
- Lighthouse スコア：Performance 90+ / Accessibility 95+ を最低ライン
- 実装はページ・セクション単位で進め、1つ完成するごとにブラウザで確認してから次に進む
- サイト内（フッター等）に `Concept Project by SakuyaLabs` を必ず明示する
- Lighthouse計測は最初から`--throttling-method=provided`（実測ベース）を使う
  （SakuyaLabs External Intelligence `PAT-004`）

## 参照
- 企画書全体：`../sakuyalabs_portfolio_plan.md`
- ①〜④の実装（テンプレートの参照元）：`../01 美容院/`, `../02 飲食店/`, `../03 ジム/`, `../04 士業/`
- Next.js 16 固有の破壊的変更に関する注意：`@AGENTS.md`

@AGENTS.md
